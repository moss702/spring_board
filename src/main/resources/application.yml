spring:
  config:
    import:
      - optional:classpath:./secret/db.yml
  application:
    name: Board
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  thymeleaf:
    cache: false
#    database-platform: org.hibernate.dialect.MariaDBDialect